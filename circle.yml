machine:
  php:
    version: 5.6.14

dependencies:
  cache_directories:
    - vendor
    - ~/cphalcon

  pre:
    - php --version

  post:
    - bin/install-phalcon.sh 2.0.x
    - php -r "echo \Phalcon\Version::get();"

test:
  override:
    - vendor/bin/phpunit